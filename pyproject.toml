[tool.poetry]
name = "expiration-reminder"
version = "0.1.0"
description = ""
authors = [
    "Hugo Puyol <chaplisimo@gmail.com>"
]
readme = "README.md"

[tool.poetry.dependencies]
python = ">=3.8"
gspread=">=6.2.1,<7.0.0"
oauth2client=">=4.1.3,<5.0.0"
python-dotenv=">=0.21.0,<0.22.0"


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[scripts]
expiration_reminder = "expiration_reminder.main:procesar_recordatorios"